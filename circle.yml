dependencies:
  override:
    - npm install -g resume-cli

test:
  override:
    - exit 0

deployment:
  release:
    branch: master
    commands:
      - resume-cli export --format html --theme flat resume.html
